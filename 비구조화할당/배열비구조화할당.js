const array = [1, 2];
const [one, two] = array;

console.log(one); // 1
console.log(two); // 2

/*
배열 비구조화 할당
- 객체 비구조화 할당과 다르게 값을 담을 변수명을 아무렇게나 해도 된다.
- 중요한 것은 배열은 순서가 있으므로 값을 담을 변수도 순서에 맞게 들어간다!!
*/